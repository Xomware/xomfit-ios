# Secrets — never commit real keys
Xomfit/Config.swift

# Xcode
*.xcuserstate
*.xcscmblueprint
xcuserdata/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store
*.swp
*~

# CocoaPods (if ever used)
Pods/

# Claude Code session memory (personal, not shared)
.claude/memory/
# Claude setup backups
.claude/.backups/
# Personal Claude overrides (not shared)
CLAUDE.local.md
.claude/settings.local.json

# Xcode build output (was tracked by mistake — 9k+ artifacts)
build/

# Claude Code agent worktrees (local scratch, not shared)
.claude/worktrees/

# Supabase CLI scratch
supabase/.temp/
build-rel/
